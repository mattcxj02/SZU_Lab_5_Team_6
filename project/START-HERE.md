# 学生项目

这个文件夹是课程中需要阅读和修改的全部源码。

- `prompt.txt`：修改机器人的身份、回答规则和表达方式。
- `knowledge.json`：增加或修改校园知识。
- `main.py`：修改后端接口和模型调用逻辑。
- `web/index.html`：修改页面结构和文字。
- `web/style.css`：修改页面样式。
- `web/app.js`：修改网页交互。

修改后，停止 CampusBot，再双击外层的 `启动 CampusBot.app`，新代码就会生效。

不要移动或重命名这个 `project` 文件夹。
